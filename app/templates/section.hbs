<div class="u__container u__section">
  <div class="u__name">{{section.name}}</div>
  <div class="u__items">
    {{#each section.items.edges as |item|}}
      {{> item item=item.node}}
    {{/each}}
  </div>
</div>

