<div class="u__item">
  <div class="u__beer-name">
    {{item.brewery}} {{item.name}}
  </div>
  <div class="u__info">
    {{item.style}}, {{item.brewery_location}}
  </div>

  {{#each item.containers.edges as |container|}}
    {{> container container=container.node}}
  {{/each}}
</div>
